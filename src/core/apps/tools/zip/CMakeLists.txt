SET(ZIP_TARGET sczip)
SET(
	ZIP_SOURCES
		main.cpp
)

INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS})

SC_ADD_EXECUTABLE(ZIP ${ZIP_TARGET})
SC_LINK_LIBRARIES(${ZIP_TARGET} ${Boost_iostreams_LIBRARIES})
SC_LINK_LIBRARIES_INTERNAL(${ZIP_TARGET} client)
