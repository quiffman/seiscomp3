SET(
	CORE_IO_RECORDS_HEADERS
		${CORE_IO_RECORDS_HEADERS}
		shrecord.h
		sacrecord.h
	)
IF (NOT WIN32)
	SET(
		CORE_IO_RECORDS_HEADERS
			${CORE_IO_RECORDS_HEADERS}
			ahrecord.h
	)
ENDIF (NOT WIN32)

IF (MSEED_FOUND)
	SET(
		CORE_IO_RECORDS_HEADERS
			${CORE_IO_RECORDS_HEADERS}
			mseedrecord.h
	)
ENDIF (MSEED_FOUND)

SC_LIB_INSTALL_HEADERS(CORE_IO_RECORDS)
