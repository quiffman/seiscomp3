IF (COMPONENT_ESSENTIALS_DB_MYSQL AND MYSQL_FOUND)
    SET(
	INTERFACES
	    ${INTERFACES} 
	    mysql
    )
ENDIF (COMPONENT_ESSENTIALS_DB_MYSQL AND MYSQL_FOUND)

IF (COMPONENT_ESSENTIALS_DB_SQLITE3 AND SQLITE3_FOUND)
    SET(
	INTERFACES
	    ${INTERFACES}
	    sqlite
    )
ENDIF (COMPONENT_ESSENTIALS_DB_SQLITE3 AND SQLITE3_FOUND)

IF (COMPONENT_ESSENTIALS_DB_POSTGRESQL AND POSTGRESQL_FOUND)
    SET(
	INTERFACES
	    ${INTERFACES}
	    postgresql
    )
ENDIF (COMPONENT_ESSENTIALS_DB_POSTGRESQL AND POSTGRESQL_FOUND)

IF (INTERFACES)
    SUBDIRS(${INTERFACES})
ENDIF (INTERFACES)
