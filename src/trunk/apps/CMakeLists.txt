IF (COMPONENT_TRUNK_PROCESSING)
	SET(PROCESSING_DIR processing)
ENDIF (COMPONENT_TRUNK_PROCESSING)

IF (SWIG_PYTHON_WRAPPERS)
	SET(PYTHON_DIR python)
ENDIF (SWIG_PYTHON_WRAPPERS)

SUBDIRS(
	${PROCESSING_DIR}
	tools
	${PYTHON_DIR}
)

SET(
	GLOBAL_CONFIG
		config/global.cfg
)

SC_INSTALL_CONFIG(GLOBAL)
