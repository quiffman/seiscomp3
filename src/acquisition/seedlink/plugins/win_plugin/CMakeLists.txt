SET(WINPLUGIN_SOURCES
	win_plugin.c
	../plugin.c
)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(..)

ADD_EXECUTABLE(win_plugin ${WINPLUGIN_SOURCES})
INSTALL(TARGETS win_plugin RUNTIME DESTINATION acquisition/bin)
