* template: #template#
plugin #pluginid# cmd="#pkgroot#/bin/nrts_plugin #daemon_opt# -v -s #srcaddr# -p #srcport#"
             timeout = 3600
             start_retry = 60
             shutdown_wait = 10

