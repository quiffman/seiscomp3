* template: #template#
plugin LISS_#statid# cmd="#pkgroot#/bin/sock_plugin -v -s #srcaddr# -p #srcport# -n #netid#"
             timeout = 600
             start_retry = 60
             shutdown_wait = 10

